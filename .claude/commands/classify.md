Classify the document at the specified path using IFMOS ML pipeline.

Steps:
1. Use the `ifmos_classify_document` tool with the file path
2. Display the classification result including:
   - Document type
   - Confidence score
   - Extracted entities (dates, names, amounts, organizations)
   - Text preview (first 200 characters)
3. Ask if the classification is correct
4. If incorrect, prompt for correct type and submit feedback

Be concise and show results in a structured format.
